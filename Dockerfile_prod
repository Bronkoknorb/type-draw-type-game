# production container

# For unknown reasons the container doesn't start up (hangs) on the Raspberry Pi when using Ubuntu 20.04 or Ubuntu 22.04
# Therefore I am sticking to 18.04 for now. We copy Java 17 from the Eclipse Temurin image.

FROM ubuntu:18.04
ENV JAVA_HOME=/opt/java/openjdk
COPY --from=eclipse-temurin:17-jre-jammy $JAVA_HOME $JAVA_HOME
ENV PATH="${JAVA_HOME}/bin:${PATH}"

WORKDIR /opt/tdt/
COPY build/server.jar server.jar
EXPOSE 8080
VOLUME /tdt-data
ENTRYPOINT ["java","-jar","server.jar","--storage.dir=/tdt-data/"]
