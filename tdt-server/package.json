{
  "scripts": {
    "format": "prettier --plugin=prettier-plugin-java --write '**/*.java'",
    "deps-update": "ncu -u && rimraf node_modules package-lock.json && npm install"
  },
  "devDependencies": {
    "npm-check-updates": "^23.1.0",
    "prettier": "3.9.6",
    "prettier-plugin-java": "2.10.3",
    "rimraf": "^6.1.3"
  }
}
