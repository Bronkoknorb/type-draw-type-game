package net.czedik.hermann.tdt;

import org.junit.jupiter.api.Test;

public class ApplicationUnitTests {

    @Test
    void dummyTest() {
        // this only exists so that we have a simple unit test that can be run first in the docker container
        // (helps to initialize the build cache)
    }
}
